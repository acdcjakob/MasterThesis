% \usepackage[utf8]{inputenc}
%%     only relevant for pdflatex

%! Colors
\usepackage{xcolor}
    \definecolor{shadecolor}{gray}{0.95}

%! images and figures
\usepackage{graphbox}
    % already loads graphicx package!
    % allows for "align=c" argument in includegraphics for aligning vertically
\usepackage{epstopdf}
    % needed for converting eps files when compiling with pdflatex


% \usepackage{wrapfig}
    % allows wrapping text around figure; but has not so good performance
\usepackage[margin=10pt,font=small,labelfont={bf}]{caption}

\graphicspath{
    {fastImages}
    {../Plots/Thesis/1/}
    {../Plots/Thesis/2/}
    {../Plots/Thesis/3/}
    {../Plots/graphics/}
    {../Plots/photographs/}
}

%! layout
\usepackage{lmodern} % thicker font (?)
% \usepackage[inner=2.5cm,outer=3.5cm,top=2cm,bottom=2cm]{geometry}
% %TODO   book style --> for final document!

\usepackage[inner=3cm,outer=3cm,top=2cm,bottom=2cm]{geometry}
%   for article style
\usepackage{booktabs}
%   better tables, toprule etc.
\usepackage{multirow}
% \usepackage{ctable}
\usepackage{framed}
    % allows framing of text
% \usepackage{fancyhdr}
%     \pagestyle{fancy}
\vbadness=3000 % no warnings vor \vbox with badness < 2000

%! Typography
\usepackage[version=4]{mhchem}
    % chemistry
\usepackage{amssymb}
\usepackage{amsmath}

%* units
\usepackage{siunitx}
    \DeclareSIUnit{\angstrom}{\textup{\AA}}
    \DeclareSIUnit{\bar}{bar}
    \DeclareSIUnit{\pulse}{pulse}
    \DeclareSIUnit{\wtpercent}{wt.\%}
    \sisetup{range-units=single}
    % \sisetup{range-phrase = \,--\,}

% greek
\usepackage{textgreek}
% lists
\usepackage{enumitem}
    % allows roman numbering
% quotes
\usepackage{csquotes}
    % convenient quotes
%* footnotes
\usepackage{fmtcount} % For formatting numbers
\usepackage[hang]{footmisc}
    % "flushmargin" as argument: align to margin
    % "hang" as argument: nice :)

% Define a new footnote command with numbers in parentheses
\renewcommand{\thefootnote}{(\arabic{footnote})}


%! Referencing
%* Table of contents
\setcounter{tocdepth}{3}
    % show subsubsection in ToC, 2 would only show until subsection
% \setcounter{secnumdepth}{3}
    % numering also subsubsections
\usepackage{minitoc}
    \noptcrule 
    % allows making TOC at begin of chapters

%* Equation referencing
\usepackage{xpatch}
\xpretocmd{\eqref}{Equ.\,}{}{}
    % this alters the \eqref command and I got still the list of labels as suggestions
%* Appendix
\usepackage[toc,page]{appendix}

%* Glossary
\usepackage[acronym]{glossaries}
% \makeglossaries % run `makeglossaries main' in terminal then compile again
    \input{tex-files/_specs/glos.tex}

%* Links
\usepackage[colorlinks=true,citecolor=blue,linkcolor=black,urlcolor=violet]{hyperref}

%! own abbreviations
\input{tex-files/_specs/abbrev.tex}

%! commenting in draft
\newcommand{\tbd}{\colorbox{magenta}{\emph{tbd}}}
\newcommand{\comment}[1]{
    \begin{shaded}
        \textsf{\underline{Comment}:
        \itshape\color{darkgray}
        #1
        }
    \end{shaded}
}

%! Library
\usepackage[style=numeric-comp,maxcitenames=2,sorting=none]{biblatex}
    %numeric-comp summerizes entries in \cite
\addbibresource{C:/Users/acdcj/OneDrive/Documents/Bibliothek/My Library.bib}
    \AtEveryBibitem{%
        \clearfield{month}%
        \clearfield{day}%
        \clearfield{urlyear}%
        \clearfield{urlmonth}%
        }
    \AtEveryCitekey{%
        \clearfield{month}%
        \clearfield{day}%
        \clearfield{urlyear}%
        \clearfield{urlmonth}%
        }

% Change the textcite command to "Name (year) [#]"
\usepackage{xpatch}
\xpatchbibmacro{textcite}
{\printnames{labelname}}
{\printnames{labelname} (\printfield{year})}
{}
{}